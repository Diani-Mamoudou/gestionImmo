<!DOCTYPE html>
<html lang="en">

	<head>

		<meta
		charset="utf-8">
		<!--[if IE]><meta http-equiv='X-UA-Compatible' content='IE=edge,chrome=1'><![endif]-->
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<meta name="description" content="">
		<meta name="author" content="">
		<link rel="shortcut icon" href="assets/img/ico/favicon.ico">
		<link rel="apple-touch-icon" sizes="144x144" href="{{asset("vendor/img/ico/apple-touch-icon-144x144.png")}}">
		<link rel="apple-touch-icon" sizes="114x114" href="{{asset("vendor/img/ico/apple-touch-icon-114x114.png")}}">
		<link rel="apple-touch-icon" sizes="72x72" href="{{asset("vendor/img/ico/apple-touch-icon-72x72.png")}}">
		<link rel="apple-touch-icon" href="{{asset("vendor/img/ico/apple-touch-icon-57x57.png")}}">

		<title>GE-IM</title>

		<!-- Bootstrap Core CSS -->
		<link href="{{asset("vendor/css/bootstrap.min.css")}}" rel="stylesheet">
		<link href="{{asset("vendor/css/animate.css")}}" rel="stylesheet">
		<link
		href="{{asset("vendor/css/plugins.css")}}" rel="stylesheet">

		<!-- Custom CSS -->
		<link
		href="{{asset("vendor/css/style.css")}}" rel="stylesheet">

		<!-- Custom Fonts -->
		<link href="{{asset("vendor/font-awesome/css/font-awesome.min.css")}}" rel="stylesheet" type="text/css">
		<link href="{{asset("vendor/css/pe-icons.css")}}" rel="stylesheet">

	</head>

	<body id="page-top" class="index">

		<div
			class="master-wrapper">

			<!-- Navigation -->
			<nav class="navbar navbar-default navbar-fixed-top fadeInDown" data-wow-delay="0.5s">
				<div
					class="container">
					<!-- Brand and toggle get grouped for better mobile display -->
					<div class="navbar-header page-scroll">
						<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-navigation">
							<span class="sr-only">Toggle navigation</span>
							<span class="icon-bar"></span>
							<span class="icon-bar"></span>
							<span class="icon-bar"></span>
						</button>
						<h1 style="color:white">GE-IM</h1>
					</div>

					<!-- Collect the nav links, forms, and other content for toggling -->
					<div class="collapse navbar-collapse" id="main-navigation">
						<ul class="nav navbar-nav navbar-right">
							{% if app.user %}
								{% if app.user.profil.libelle == "Gestionnaire" %}
									<li class="dropdown">
										<a href="{{path("biens_shows")}}">Acceuil</a>
									</li>
								{% else %}
									<li class="dropdown">
										<a href="{{path("front_shows")}}">Acceuil</a>
									</li>
								{% endif %}
							{% else %}
								<li class="dropdown">
									<a href="{{path("front_shows")}}">Acceuil</a>
								</li>
							{% endif %}

							{% if app.user !=null %}
								{% if app.user.profil.libelle == "Gestionnaire" %}
									<li class="dropdown">
										<a href="#" class="dropdown-toggle" data-hover="dropdown" data-toggle="dropdown">gestion demande<span class="pe-7s-angle-down"></span>
										</a>
										<ul class="dropdown-menu" role="menu">
											<li>
												<a href="{{path("touteDemande")}}">Demande</a>
											</li>
											<li>
												<a href="{{path("touteReservation")}}">Reservation</a>
											</li>
										</ul>
									</li>

									<li class="dropdown">
										<a href="#" class="dropdown-toggle" data-hover="dropdown" data-toggle="dropdown">Gestion d'utilisateur<span class="pe-7s-angle-down"></span>
										</a>
										<ul class="dropdown-menu" role="menu">
											<li>
												<a href="{{path("user_by_profil", {profil:"proprietaire"})}}">Propriétaires</a>
											</li>
											<li>
												<a href="{{path("user_by_profil", {profil:"client"})}}">Clients</a>
											</li>
										</ul>
									</li>
								{% endif %}

								{% if app.user.profil.libelle == "Proprietaire" %}

									<li class="dropdown">
										<a href="{{path("biens_shows_user",{id:app.user.id})}}">Mes biens</a>
									</li>
									<li class="dropdown">
										<a href="{{path("newDemande", {id:app.user.id})}}">Nouvelle demande</a>
									</li>

								{% endif %}

								{% if app.user.profil.libelle == "Client" %}
									<li class="dropdown">
										<a href="{{path("demande_shows_user",{id:app.user.id})}}">Mes reservations</a>
									</li>

								{% endif %}
							{% endif %}

							<li class="dropdown">
								<a href="#" class="dropdown-toggle" data-hover="dropdown" data-toggle="dropdown">Catégorie
									<span class="pe-7s-angle-down"></span>
								</a>
								<ul class="dropdown-menu" role="menu">
									<li>
										<a href="{{path("bien_by_type", {type:"immeuble"})}}">Immeuble</a>
									</li>
									<li>
										<a href="{{path("bien_by_type", {type:"chambre"})}}">Chambre</a>
									</li>
									<li>
										<a href="{{path("bien_by_type", {type:"studio"})}}">Studio</a>
									</li>
									<li>
										<a href="{{path("bien_by_type", {type:"appartement"})}}">Appartement</a>
									</li>
								</ul>
							</li>
							<li class="dropdown">
								{% if app.user %}
									<li class="dropdown">
										<a href="#" class="dropdown-toggle" data-hover="dropdown" data-toggle="dropdown">{{ app.user.email}}
											<span class="pe-7s-angle-down"></span>
										</a>
										<ul class="dropdown-menu" role="menu">
											<li>
												<a href="{{path("mon_profil",{id:app.user.id})}}">mon profil</a>
											</li>
											<li>
												<a href="{{path("app_logout")}}">se deconnecter</a> 
											</li>
										</ul>
									</li>
								{% else %}
									<li class="dropdown">
										<a href="#" class="dropdown-toggle" data-hover="dropdown" data-toggle="dropdown">Connection
											<span class="pe-7s-angle-down"></span>
										</a>
										<ul class="dropdown-menu" role="menu">
											<li>
												<a href="{{path("app_login")}}">Se connecter</a>
											</li>
											<li>
												<a href="{{path("app_register")}}">Creer compte</a>
											</li>
										</ul>
									</li>
								{% endif %}

								<!-- si admin ou proprio ou client-->
								<span class="pe-7s-angle-down"></span>
							</li>
						</a>

						{% if app.user %}{% endif %}

					</li>
				</ul>
			</div>
			<!-- /.navbar-collapse -->
		</div>
		<!-- /.container-fluid -->
	</body>
</html></nav><!-- Header --><header id="headerwrap" class="backstretched special-max-height">
<div class="container vertical-center">
	{% block header %}
		<div class="intro-text vertical-center text-left smoothie">
			<div class="intro-heading wow fadeIn heading-font" data-wow-delay="0.2s">c'est GE-IM</div>
			<div class="intro-sub-heading wow fadeIn secondary-font" data-wow-delay="0.4s">Avec nous
				<span class="rotate">gérer vos biens, louer des biens, simplifier la gestion de biens</span>
			</div>
		</div>
	{% endblock %}

</div></header>

<section>
{% block body %}{% endblock %}
</section></div>





{% block js %}
<script src="{{asset("vendor/js/jquery.js")}}"></script>
<script src="{{asset("vendor/js/bootstrap.min.js")}}"></script>
<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
<script src="{{asset("vendor/js/init.js")}}"></script>
    <script src="{{asset("vendor/js/plugins.js")}}"></script>

<!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries --><!-- WARNING: Respond.js doesn't work if you view the page via file:// --><!--[if lt IE 9]>
			        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
			        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
			    <![endif]--><script type="text/javascript"> $(document).ready(function () {
'use strict';
jQuery('#headerwrap').backstretch([
"https://source.unsplash.com/1280x853/?immeuble",
], {
duration: 8000,
fade: 500
});
});</script>

{% endblock %}


</body></html>
